# Swift / SwiftPM
.build/
.swiftpm/
*.xcodeproj
*.xcworkspace
DerivedData/

# macOS
.DS_Store

# Node / Deno
node_modules/

# Supabase local state
supabase/.temp/
supabase/.branches/

# Secrets — never commit
.env
.env.*
*.pem
*.p8

# Logs & scratch
*.log
.scratch/
App/Configs/Supabase.xcconfig

# Web: the operator's own Supabase URL/key go here (publishable key is public,
# but the project URL is theirs to publish, not ours to commit).
web/config.local.js
